on:
  pull_request:
    types:
      - labeled
      - unlabeled
      - synchronize
      - opened
      - edited
      - ready_for_review
      - reopened
      - unlocked
  pull_request_review:
    types:
      - submitted
  check_suite:
    types:
      - completed
  status: {}

jobs:
  automerge:
    uses: nolte/gh-plumbing/.github/workflows/reusable-automerge.yaml@3fccbb333146cd2a65902afd7394db870ae6a978 # v2.1.0
    with:
      # Mint a portfolio-App token so the action's branch auto-updates are
      # pushed under the App identity. Pushes made with GITHUB_TOKEN do not
      # trigger workflows (GitHub's recursion guard), which leaves branch
      # auto-updates without any status checks and stalls PRs in a
      # BLOCKED/"no checks" loop.
      app-id: ${{ vars.PORTFOLIO_APP_ID }}
    secrets:
      token: ${{ secrets.GITHUB_TOKEN }}
      app-private-key: ${{ secrets.PORTFOLIO_APP_PRIVATE_KEY }}
    permissions:
      contents: write
      pull-requests: write
