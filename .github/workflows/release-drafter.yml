on:
  push:
    branches: [develop]

jobs:
  update_release_draft:
    uses: nolte/gh-plumbing/.github/workflows/reusable-release-drafter.yml@3fccbb333146cd2a65902afd7394db870ae6a978 # v2.1.0
    secrets:
      token: ${{ secrets.GITHUB_TOKEN }}
    permissions:
      contents: write
      pull-requests: write
